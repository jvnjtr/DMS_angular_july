import { MetasearchPipe } from './metasearch.pipe';

describe('MetasearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MetasearchPipe();
    expect(pipe).toBeTruthy();
  });
});
